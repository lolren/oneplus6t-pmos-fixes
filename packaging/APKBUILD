# Local-checkout package recipe. See packaging/README.md before using in aports.
maintainer="Lolren <loren.bufanu@gmail.com>"
pkgname=oneplus6t-pmos-fixes
pkgver=0.1.0
pkgrel=38
pkgdesc="Safe postmarketOS connectivity and camera helpers for OnePlus 6T"
url="https://github.com/lolren/oneplus6t-pmos-fixes"
arch="noarch"
license="MIT"
depends="
	cmd:getent
	curl
	iproute2
	mobile-broadband-provider-info
	modemmanager
	networkmanager-cli
	pipewire-pulse-systemd
	postmarketos-base-ui-audio-backend-pipewire
	pulseaudio-utils
	python3
	wireplumber
	"
checkdepends="git"
options="!strip"
builddir="$startdir/.."

check() {
	make -C "$builddir" test
}

package() {
	make -C "$builddir" \
		DESTDIR="$pkgdir" \
		PREFIX=/usr \
		install
}
